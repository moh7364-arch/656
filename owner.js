
window.addEventListener('DOMContentLoaded',()=>{ const listings=getOwnerListings(); const bookings=getBookings(); qs('#ownerListingsCount').textContent=listings.length; qs('#ownerBookingsCount').textContent=bookings.length; qs('#ownerRevenueCount').textContent=formatCurrency(getPayments().reduce((a,b)=>a+b.amount,0)); qs('#ownerListings').innerHTML=(listings.length?listings:getProperties().slice(0,4)).map(p=>`<div class="panel"><div class="row-between"><strong>${p.name}</strong><span class="price">${formatCurrency(p.price)}</span></div><div class="meta-list"><span class="meta-item">${p.city}</span><span class="meta-item">${p.type}</span></div><p class="muted">${p.description}</p></div>`).join(''); qs('#ownerForm').addEventListener('submit',e=>{e.preventDefault(); const f=new FormData(e.target); addOwnerListing({name:f.get('name'),city:f.get('city'),type:f.get('type'),price:Number(f.get('price')),guests:Number(f.get('guests')),pool:f.get('pool')==='yes',location:f.get('location'),description:f.get('description')}); qs('#ownerMessage').innerHTML='<div class="notice">تمت إضافة العقار بنجاح.</div>'; e.target.reset(); setTimeout(()=>location.reload(),700); }); });


function generateReceiptPdf(booking){ const { jsPDF } = window.jspdf; const doc = new jsPDF({unit:'pt',format:'a4'}); let y=48; doc.setFont('helvetica','bold'); doc.setFontSize(21); doc.text('Tasha Booking Receipt / سند حجز طشّة',40,y); y+=28; doc.setFont('helvetica','normal'); doc.setFontSize(12); [['Receipt',booking.receiptNo],['Property',booking.propertyName],['City',booking.city],['Guest',booking.guestName],['Phone',booking.phone],['Dates',`${booking.startDate} - ${booking.endDate}`],['Nights',String(booking.nights)],['Payment',booking.paymentType==='deposit'?'Deposit':'Full Payment'],['Amount Paid',`${booking.amount} JOD`],['Booking Total',`${booking.total} JOD`],['Status',booking.status]].forEach(([k,v])=>{ doc.text(`${k}: ${v}`,40,y); y+=18; }); doc.line(40,y,555,y); y+=24; doc.text('Tasha Jordan - Under supervision of Eng. Malik',40,y); doc.save(`${booking.receiptNo}.pdf`); }
window.addEventListener('DOMContentLoaded',()=>{ const bookingId=Number(new URLSearchParams(location.search).get('bookingId')); const booking=getBookings().find(x=>x.id===bookingId); const summary=qs('#paymentSummary'); if(!booking){ summary.innerHTML='<div class="empty-state">لم يتم العثور على الحجز المطلوب.</div>'; return;} summary.innerHTML=`<div class="section-head"><span class="section-kicker">ملخص الحجز</span><h1 style="margin:0">${booking.propertyName}</h1><p>${booking.city} • ${booking.receiptNo}</p></div><div class="summary-list"><div class="summary-item"><span>الضيف</span><strong>${booking.guestName}</strong></div><div class="summary-item"><span>الدخول</span><strong>${booking.startDate}</strong></div><div class="summary-item"><span>الخروج</span><strong>${booking.endDate}</strong></div><div class="summary-item"><span>الإجمالي</span><strong>${formatCurrency(booking.total)}</strong></div><div class="summary-item"><span>المبلغ المطلوب الآن</span><strong>${formatCurrency(booking.amount)}</strong></div></div>`; qs('#paymentForm').addEventListener('submit',e=>{e.preventDefault(); const f=new FormData(e.target); savePayment({bookingId:booking.id,amount:booking.amount,method:f.get('method'),cardName:f.get('cardName')}); qs('#paymentMessage').innerHTML=`<div class="notice">تم تأكيد الدفع بنجاح. يمكنك الآن مراجعة الحجز أو تنزيل السند مباشرة.</div><div class="card-actions" style="margin-top:12px"><a class="btn btn-secondary" href="bookings.html">الانتقال إلى الحجوزات</a><button type="button" class="btn btn-accent" id="downloadReceiptNow">تنزيل السند PDF</button></div>`; qs('#downloadReceiptNow').onclick=()=>generateReceiptPdf({...booking,status:'approved'}); }); })

window.addEventListener('DOMContentLoaded',()=>{ qs('#loginForm').addEventListener('submit',e=>{e.preventDefault(); const email=new FormData(e.target).get('email'); const user=getUsers().find(u=>u.email===email)||getUsers()[0]; writeStore(STORAGE.current,user); qs('#loginMessage').innerHTML='<div class="notice">تم تسجيل الدخول بنجاح.</div>'; setTimeout(()=>location.href='index.html',600); }); });

function renderAvailabilityCalendar(propertyId){ const base=new Date(2026,3,1), y=base.getFullYear(), m=base.getMonth(); const days=new Date(y,m+1,0).getDate(); const dayNames=['أحد','إثنين','ثلاثاء','أربعاء','خميس','جمعة','سبت']; let html=dayNames.map(n=>`<div class="calendar-day header">${n}</div>`).join(''); for(let d=1; d<=days; d++){ const ds=`${y}-${String(m+1).padStart(2,'0')}-${String(d).padStart(2,'0')}`; const blocked=isDateBlocked(propertyId,ds); html += `<div class="calendar-day ${blocked?'blocked':'available'}"><div>${d}</div><small>${blocked?'محجوز':'متاح'}</small></div>`; } return {title:`${base.toLocaleDateString('ar-JO',{month:'long'})} ${y}`,grid:html}; }
window.addEventListener('DOMContentLoaded',()=>{ const property=getPropertyById(new URLSearchParams(location.search).get('id')); const container=qs('#propertyContainer'); if(!property){ container.innerHTML='<div class="empty-state">العقار غير موجود.</div>'; return;} const deposit=Math.round(property.price*.3); const cal=renderAvailabilityCalendar(property.id); container.innerHTML=`<div class="property-layout"><div><div class="property-gallery"><img class="main-image" src="${property.gallery[0]}" alt="${property.name}">${property.gallery.slice(1,4).map(i=>`<img src="${i}" alt="${property.name}">`).join('')}</div><div class="panel" style="margin-top:16px"><div class="row-between"><div><span class="section-kicker">${property.type}</span><h1 style="font-size:2.1rem;margin:10px 0 0">${property.name}</h1><p>${property.location}</p></div><button id="favBtn" class="btn btn-secondary btn-small">${getFavorites().includes(property.id)?'إزالة من المفضلة':'إضافة للمفضلة'}</button></div><div class="meta-list"><span class="meta-item">⭐ ${property.rating}</span><span class="meta-item">${property.guests} أشخاص</span><span class="meta-item">${property.area}</span><span class="meta-item">${property.pool?'مسبح خاص':'جلسة خارجية'}</span></div><p>${property.description}</p><div class="amenities">${property.amenities.map(a=>`<div class="amenity">${a}</div>`).join('')}</div><div class="notice">مناسب لـ: ${property.categoryTags.join('، ')}</div></div><div class="property-extra-grid"><div class="panel"><div class="row-between"><h3 style="margin:0">تقويم التوفر</h3><span class="badge">${cal.title}</span></div><div class="calendar-grid">${cal.grid}</div></div><div class="panel"><h3 style="margin-top:0">اسأل عن هذا العقار</h3><div class="chat-window" id="aiResult"><div class="chat-bubble ai">يمكنني مساعدتك في تقييم هذا العقار بالنسبة لميزانيتك أو عدد الضيوف.</div></div><label>رسالتك<textarea id="aiPrompt" rows="4" placeholder="مثال: هل هذا العقار مناسب لعائلة من 8 أشخاص؟"></textarea></label><button id="runAiBtn" class="btn btn-primary btn-block">إرسال</button></div></div></div><aside class="sticky-box"><div class="panel"><div class="row-between"><strong style="font-size:1.5rem">${formatCurrency(property.price)}</strong><span class="badge">لليلة الواحدة</span></div><div class="summary-list"><div class="summary-item"><span>العربون المقترح</span><strong>${formatCurrency(deposit)}</strong></div><div class="summary-item"><span>المدينة</span><strong>${property.city}</strong></div><div class="summary-item"><span>السعة</span><strong>${property.guests} أشخاص</strong></div></div><form id="bookingForm" class="form-grid" style="grid-template-columns:1fr;margin-top:14px"><label>الاسم الكامل<input name="guestName" required placeholder="مثال: أحمد علي"></label><label>رقم الهاتف<input name="phone" required placeholder="07xxxxxxxx"></label><label>تاريخ الدخول<input type="date" name="startDate" required></label><label>تاريخ الخروج<input type="date" name="endDate" required></label><label>نوع الدفع<select name="paymentType"><option value="full">دفع كامل</option><option value="deposit">عربون فقط</option></select></label><button class="btn btn-primary btn-block" type="submit">متابعة إلى الدفع</button></form><div id="bookingMessage" style="margin-top:12px"></div></div></aside></div>`; qs('#favBtn').onclick=()=>{toggleFavorite(property.id); updateCounters(); location.reload()}; qs('#bookingForm').addEventListener('submit',e=>{e.preventDefault(); const f=new FormData(e.target); const start=f.get('startDate'), end=f.get('endDate'); const nights=nightsBetween(start,end), total=property.price*nights, paymentType=f.get('paymentType'), amount=paymentType==='deposit'?Math.round(total*.3):total; if(isDateBlocked(property.id,start)||isDateBlocked(property.id,end)){ qs('#bookingMessage').innerHTML='<div class="empty-state">أحد التاريخين المحددين محجوز. اختر تاريخًا آخر.</div>'; return;} const booking=saveBooking({propertyId:property.id,propertyName:property.name,city:property.city,guestName:f.get('guestName'),phone:f.get('phone'),startDate:start,endDate:end,nights,paymentType,amount,total}); qs('#bookingMessage').innerHTML='<div class="notice">تم حفظ الطلب. سيتم تحويلك إلى صفحة الدفع الآن.</div>'; setTimeout(()=>location.href=`payment.html?bookingId=${booking.id}`,600); }); setupAiChat(); });
